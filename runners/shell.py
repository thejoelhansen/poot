
# TODO

def run(cmd):
    print(f"Running: {cmd}")
    # your bash parsing logic here
    return f"Executed {cmd}"

    # If app <command> --help is present then display help text & exit

    # Take cmd[body] and split it and then shove it into run.subprocess for clean execution
